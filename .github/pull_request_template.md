## What changed

Describe the user-facing or developer-facing change in a few sentences.

## Route / area

- App/route:
- Shared component/API/database area:

## Why

What problem does this solve?

## Testing

- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] direct route + hard reload tested
- [ ] signed-out/public behavior tested if relevant
- [ ] signed-in behavior tested if relevant
- [ ] PWA install/update behavior tested if relevant
- [ ] API/database failure state tested if relevant

Build fingerprint tested:

`vX.Y.Z · abc1234 · ...`

## Visual changes

Attach before/after screenshots for material UI changes.

## Security / data impact

- Does this touch auth, RLS, roles, storage, environment variables, or server-side provider calls?
- If yes, explain the boundary and why it remains safe.

## Checklist

- [ ] branch name follows `feat/`, `fix/`, `docs/`, `refactor/`, `chore/`, or `security/`
- [ ] change is focused and reviewable
- [ ] shared UI/registry conventions were reused
- [ ] no secrets, tokens, cookies, or private URLs are included
- [ ] substantial app documentation was updated
- [ ] no fake live data was introduced
